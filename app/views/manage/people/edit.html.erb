<% content_for :title, "#{@person.display_name} の編集" %>
<h1 class="font-display text-2xl"><%= @person.display_name %> の編集</h1>
<%= render "form", person: @person %>
<p class="mt-6"><%= link_to "メンバー一覧に戻る", manage_people_path, class: "text-sm text-muted" %></p>
