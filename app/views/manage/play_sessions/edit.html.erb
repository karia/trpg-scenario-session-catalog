<% content_for :title, "#{PlaySession.model_name.human}の編集" %>
<h1 class="font-display text-2xl"><%= @play_session.scenario&.title || PlaySession.model_name.human %> の編集</h1>
<%= render "form", play_session: @play_session %>
